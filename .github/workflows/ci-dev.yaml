name: CI Pipeline - Dev Branch

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Setup DVC
      run: |
        dvc remote list
    
    - name: Pull DVC tracked files
      run: |
        dvc pull || echo "No DVC files to pull yet"
    
    - name: Train model if not exists
      run: |
        python src/train.py
    
    - name: Track model with DVC
      run: |
        dvc add models/iris_model.pkl || echo "Model already tracked"
    
    - name: Run Data Validation Tests
      run: |
        pytest tests/test_data_validation.py -v
    
    - name: Run Evaluation Tests
      run: |
        pytest tests/test_evaluation.py -v
